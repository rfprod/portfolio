/*
*	SystemJS Extrac configuration for Angular
*/
(function (global) { // eslint-disable-line no-unused-vars
	System.config({
		// Map something
		map: {
			//'appDir': './base/public/app'
		},
	});
})(this);
